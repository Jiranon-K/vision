// The editor feature's interface (ADR 0007): the Creator's writing surface for
// one Post. Excerpt Suggestion's frontend lives inside it; nothing else calls
// it yet.
export { default as PostEditorForm } from "./components/post-editor-form";
