"use client";

export interface EditorModeSwitchSlotProps {
  className?: string;
}

// Empty by design. Ticket 02 ("The writing surface itself") fills this with
// the Write / Split / Preview switch.
export default function EditorModeSwitchSlot() {
  return null;
}
